# MCP Server Dependencies
mcp>=1.14.0

# Core dependencies
anyio>=4.5
httpx>=0.27.1
httpx-sse>=0.4
jsonschema>=4.20.0
pydantic>=2.11.0
pydantic-settings>=2.5.2
python-multipart>=0.0.9
sse-starlette>=1.6.1
starlette>=0.27
uvicorn>=0.31.1

# Optional: For the HTTP server version (if you want to keep it)
flask>=2.0.0
